tests:
  "Test mocha context http-bat":
    GET /:
      timeout: 10000
      description: Request GET https://github.com 
      response:
        headers:
          connection: !!variable connectionHeader